# 企微管理V2.0 — Acquisition.vue 编译错误修复

> **日期**: 2026-04-13
> **模块**: Phase 12-B 获客助手 (Acquisition.vue)
> **状态**: ✅ 已完成

---

## 一、问题描述

在继续执行 Phase 12 后续任务时，检测到 `Acquisition.vue` 存在多处 TypeScript / ESLint 编译错误，均为 Phase 12-B 重写时添加了模板代码但对应的 script 变量/函数声明未补全。

---

## 二、错误清单

### 2.1 TypeScript 错误（TS2339 - Property does not exist）

| 行号 | 缺失变量/方法 | 所在位置 |
|------|------------|---------|
| 94 | `quickDateRange` | 渠道分析 Tab — 快捷日期筛选 radio-group |
| 94 | `handleQuickDateChange` | 渠道分析 Tab — 快捷筛选 change 事件 |
| 115 | `handleCustomDateChange` | 渠道分析 Tab — 自定义日期 change 事件 |
| 218 | `scheduleEnabled` | 权重配置 Tab — 智能调度开关 |
| 221 | `scheduleEnabled` | 权重配置 Tab — v-if 条件 |
| 224/226/229/234/239/240 | `scheduleConfig` | 权重配置 Tab — 智能调度表单 |
| 230 | `weekdayOptions` | 权重配置 Tab — 工作日复选框 |
| 262 | `handleAddTagGroup` | 标签管理 Tab — 创建标签组按钮 |
| 266 | `displayTagGroups` | 标签管理 Tab — 表格 data |
| 284 | `handleEditTagGroup` | 标签管理 Tab — 编辑按钮 |
| 285 | `handleDeleteTagGroup` | 标签管理 Tab — 删除按钮 |

### 2.2 ESLint 未使用变量错误

| 变量名 | 问题 |
|-------|------|
| `handleChannelDateChange` | 定义后从未调用（被新的 `handleQuickDateChange` 和 `handleCustomDateChange` 取代） |
| `barHeight` | CSS柱状图辅助函数，模板改为 ECharts 后遗留 |
| `singleBarH` | 同上 |
| `channelTrendMax` | 同上 |

### 2.3 缺失导入

| 缺失项 | 类型 |
|-------|------|
| `Calendar` | @element-plus/icons-vue 图标 |
| `reactive` | vue 核心 API |
| `echarts` | ECharts 库 (`import * as echarts from 'echarts'`) |

---

## 三、修复内容

### 3.1 修复导入

```typescript
// Before
import { ref, computed, onMounted, watch, nextTick } from 'vue'
import { Plus, Download } from '@element-plus/icons-vue'
// After
import { ref, computed, reactive, onMounted, watch, nextTick } from 'vue'
import { Plus, Download, Calendar } from '@element-plus/icons-vue'
import * as echarts from 'echarts'
```

### 3.2 删除未使用变量

- 删除 `handleChannelDateChange`
- 删除 `barHeight`、`singleBarH`、`channelTrendMax`

### 3.3 新增缺失变量/函数

#### 日期快捷筛选
```typescript
const quickDateRange = ref('')
const handleQuickDateChange = (val: string) => { /* 今日/7天/30天 */ }
const handleCustomDateChange = () => { quickDateRange.value = 'custom'; ... }
```

#### 智能调度
```typescript
const scheduleEnabled = ref(false)
const scheduleConfig = reactive({ startTime, endTime, weekdays, offAction, fallbackUserId })
const weekdayOptions = [{ value: 1, label: '一' }, ... , { value: 0, label: '日' }]
```

#### 标签管理
```typescript
const tagGroups = ref<any[]>([])
const DEMO_TAG_GROUPS_LOCAL = [/* 3组示例标签 */]
const displayTagGroups = computed(...)
const handleAddTagGroup = () => { /* 示例模式提示 */ }
const handleEditTagGroup = (_row: any) => { /* 示例模式提示 */ }
const handleDeleteTagGroup = (_row: any) => { /* 示例模式提示 */ }
```

#### ECharts 趋势图
```typescript
const trendChartRef = ref<HTMLDivElement>()
const channelTrendChartRef = ref<HTMLDivElement>()
let trendChartInstance: echarts.ECharts | null = null
let channelTrendChartInstance: echarts.ECharts | null = null
// initTrendChart() — 初始化汇总趋势图（折线图）
// initChannelTrendChart() — 初始化单渠道趋势图（柱状图）
// watch(trendChartData, ...) — 数据变化时自动更新
// watch(channelTrendVisible, ...) — 弹窗打开/关闭时 init/dispose
```

---

## 四、修改文件

| 文件 | 改动类型 | 说明 |
|------|---------|------|
| `src/views/Wecom/Acquisition.vue` | 修复 | 添加缺失导入、变量声明、函数实现；删除未使用变量 |

---

## 五、验收

- [x] `get_errors` 检查：所有编译错误（ERROR 400级）清零
- [x] `reactive`、`Calendar`、`echarts` 正确导入
- [x] 所有模板中引用的变量均已声明
- [x] 所有未使用变量已删除
- [x] ECharts 趋势图完整实现（含 watch + dispose 逻辑）
- [x] 标签管理 Tab 的增/改/删按钮在示例模式下有友好提示

---

## 六、后续建议

- 标签管理 Tab 目前示例模式可展示 3 组示例标签，真实模式下需接入 `/wecom/tag-groups` API
- 智能调度功能目前为 UI 预留，实际保存逻辑需扩展 `saveWeightConfig` 接入调度参数
- ECharts 趋势图在渠道分析 Tab 切换到对应数据后会自动渲染，真实模式下通过 `fetchChannelStats()` 获取真实数据后同步更新


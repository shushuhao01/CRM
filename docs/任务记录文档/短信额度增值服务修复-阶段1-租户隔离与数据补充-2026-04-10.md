# 短信额度增值服务修复 — 阶段1：租户隔离安全修复 + 数据补充

> **日期**: 2026-04-10  
> **总阶段**: 3个阶段  
> **当前**: 阶段1 已完成 ✅

---

## 一、任务概述

短信额度功能已有约85%实现，本阶段专注于修复租户隔离安全漏洞、补充数据库字段、增加通知机制。

---

## 二、修复清单

### 🔴 P1-P4 租户隔离安全漏洞修复（高优先级）

| 编号 | 问题 | 文件 | 修复内容 |
|------|------|------|---------|
| P1 | CRM端订单查询无租户隔离 | `smsQuota.ts` L207 | `/quota/order/:orderNo` 查询条件增加 `tenantId` |
| P2 | 会员中心订单查询无租户隔离 | `member-sms-quota.ts` L165 | `/order/:orderNo` 查询条件增加 `tenantId` |
| P3 | CRM端模拟支付无租户隔离 | `smsQuota.ts` L243 | `/quota/simulate-pay/:orderNo` 查询条件增加 `tenantId` |
| P4 | 会员中心模拟支付无租户隔离 | `member-sms-quota.ts` L196 | `/simulate-pay/:orderNo` 查询条件增加 `tenantId` |

**修复方式**: 所有订单查询/操作接口统一加 `where: { orderNo, tenantId }` 双重校验。

### 🟡 数据补充 — tenant_name 字段

| 文件 | 修改 |
|------|------|
| `SmsQuotaOrder.ts` | Entity新增 `tenantName` 字段映射 `tenant_name` |
| `admin/sms-quota.ts` 建表SQL | CREATE TABLE 增加 `tenant_name VARCHAR(200)` 列 |
| `admin/sms-quota.ts` 兼容逻辑 | 自动检测并 ALTER TABLE 补充字段（旧表兼容） |
| `smsQuota.ts` 创建订单 | 创建订单时查询并保存 `tenantName` |
| `member-sms-quota.ts` 创建订单 | 创建订单时保存 `tenantName` |

### 🟡 管理后台订单展示增强

| 文件 | 修改 |
|------|------|
| `admin/sms-quota.ts` 订单列表 | 自动补充缺少tenantName的订单（查tenants表） |
| `admin/sms-quota.ts` 统计SQL | 改为按筛选条件统计，而非全局统计 |
| `QuotaPackages.vue` | 订单列表`租户ID`列改为双行展示`租户名称+ID`，增加tenantId搜索 |

### 🟡 通知机制

| 文件 | 修改 |
|------|------|
| `AdminNotificationService.ts` | EVENT_TYPES新增 `sms_quota_purchased`、`sms_quota_refunded` |
| `smsQuota.ts` 模拟支付 | 支付成功后调用 `notify('sms_quota_purchased', ...)` |
| `member-sms-quota.ts` 模拟支付 | 支付成功后调用 `notify('sms_quota_purchased', ...)` |
| `admin/sms-quota.ts` 退款 | 退款成功后调用 `notify('sms_quota_refunded', ...)` |

### 🟡 订单过期自动关闭

| 文件 | 修改 |
|------|------|
| `admin/sms-quota.ts` ensureTables | 启动时自动关闭所有 `status=pending` 且 `expire_time < NOW()` 的过期订单 |

---

## 三、修改文件清单

| 序号 | 文件路径 | 修改类型 |
|------|---------|---------|
| 1 | `backend/src/entities/SmsQuotaOrder.ts` | 新增 tenantName 字段 |
| 2 | `backend/src/routes/smsQuota.ts` | 租户隔离修复 + tenantName + 通知 |
| 3 | `backend/src/routes/public/member-sms-quota.ts` | 租户隔离修复 + tenantName + 通知 |
| 4 | `backend/src/routes/admin/sms-quota.ts` | 建表补字段 + 订单查询增强 + 通知 + 过期关闭 |
| 5 | `backend/src/services/AdminNotificationService.ts` | 新增事件类型 |
| 6 | `admin/src/views/sms-management/QuotaPackages.vue` | 租户名称展示 + 搜索 |

---

## 四、数据库自动建表说明

- 表 `sms_quota_packages` 和 `sms_quota_orders` 在管理后台路由加载时自动 `CREATE TABLE IF NOT EXISTS`
- 旧表兼容：自动检测 `tenant_name` 字段是否存在，不存在则 `ALTER TABLE ADD COLUMN`
- **无需手动执行迁移脚本，服务启动3秒后自动完成建表**

---

## 五、阶段规划

| 阶段 | 内容 | 状态 |
|------|------|------|
| **阶段1** | 租户隔离安全修复 + 数据补充 + 通知 + 自动建表 | ✅ 已完成 |
| **阶段2** | 管理后台退款调用支付API + 前端体验优化 | ⏳ 待确认 |
| **阶段3** | 审查报告更新 + 任务记录归档 | ⏳ 待确认 |


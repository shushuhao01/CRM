# 通话管理模块深度审查修复执行记录 - 2026-04-09

> **执行日期**：2026年4月9日
> **基于文档**：通话管理模块深度审查与优化建议

---

## 修复清单

### P0 — 必须修复

| # | 问题 | 涉及文件 | 修复内容 | 状态 |
|---|------|---------|---------|------|
| 1 | AliyunCallService 租户隔离缺失 | `services/AliyunCallService.ts` | 所有SQL查询添加 tenantRawSQL() 过滤：getLineConfig/makeCall/hangupCall/handleStatusCallback/handleRecordingCallback | ✅ 完成 |
| 2 | MobileWebSocketService 表名错误 | `services/MobileWebSocketService.ts` | handleCallStatus: `UPDATE calls` → `UPDATE call_records`，`status` → `call_status`；handleCallEnded: 删除不存在的 `UPDATE calls`，仅保留 `UPDATE call_records` | ✅ 完成 |
| 3 | Call 实体缺失字段 | `entities/Call.ts` | 新增6个字段映射：callMethod→call_method, lineId→line_id, callerNumber→caller_number, recordingSize→recording_size, providerCallId→provider_call_id, hangupCause→hangup_cause；callStatus枚举新增 'pending','calling' | ✅ 完成 |
| 4 | records.ts GET / 缺少权限过滤 | `routes/calls/records.ts` | 添加与 /statistics 路由一致的角色权限过滤：super_admin/admin可看所有+筛选，department_manager看本部门，salesperson只看自己 | ✅ 完成 |
| 5 | phone_blacklist UNIQUE约束 | `database/schema.sql` + 迁移脚本 | UNIQUE(phone) → UNIQUE(phone, tenant_id)，支持不同租户添加相同黑名单号码 | ✅ 完成 |
| 6 | global_call_config UNIQUE约束 | `database/schema.sql` + 迁移脚本 | UNIQUE(config_key) → UNIQUE(config_key, tenant_id)，支持不同租户独立配置 | ✅ 完成 |

### P1 — 高优先级

| # | 问题 | 涉及文件 | 修复内容 | 状态 |
|---|------|---------|---------|------|
| 7 | 工作手机设备离线静默成功 | `routes/callConfig.ts` | 设备离线返回503错误+明确提示；无device_id返回400错误+绑定提示；WebSocket发送失败返回503 | ✅ 完成 |

### 数据库迁移

| 文件 | 说明 |
|------|------|
| `database-migrations/2026-04-09-call-module-fixes.sql` | phone_blacklist/global_call_config唯一约束修复 + call_records补充provider_call_id/hangup_cause字段 |

---

## 修改文件清单

| 文件 | 修改类型 | 说明 |
|------|---------|------|
| `backend/src/entities/Call.ts` | 修改 | 新增6个字段映射，枚举扩展 |
| `backend/src/services/AliyunCallService.ts` | 修改 | 全面租户隔离：import tenantHelpers，6处SQL添加tenant过滤 |
| `backend/src/services/MobileWebSocketService.ts` | 修改 | 修复表名calls→call_records，修复列名status→call_status，删除重复UPDATE |
| `backend/src/routes/calls/records.ts` | 修改 | GET / 路由添加角色权限过滤 |
| `backend/src/routes/callConfig.ts` | 修改 | 工作手机呼叫设备离线降级处理 |
| `database/schema.sql` | 修改 | 唯一约束修复+补充字段 |
| `backend/database-migrations/2026-04-09-call-module-fixes.sql` | 新增 | 生产环境迁移脚本 |

---

## 验证

- ✅ TypeScript 编译通过（npx tsc --noEmit），修改文件零错误
- ✅ 所有修改向后兼容（租户过滤使用可选参数，私有部署模式无影响）


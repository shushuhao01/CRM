# 官网会员中心AI助手套餐映射问题修复

**日期：** 2026-04-22  
**问题：** 官网会员中心的AI助手页面没有显示管理后台配置的套餐

---

## 一、问题分析

### 1.1 前端调用流程

**文件：** `website/src/views/member/wecom/WecomAiAssistant.vue`

```typescript
// 第189-201行
const fetchPackages = async () => {
  try {
    const token = getMemberToken()
    const res = await fetch('/api/v1/wecom/ai/packages', { 
      headers: { 'Authorization': `Bearer ${token}` } 
    })
    const json = await res.json()
    if (json.success && json.data) {
      plans.value = json.data
      if (plans.value.length > 0 && !selectedPlan.value) {
        selectedPlan.value = plans.value[Math.min(2, plans.value.length - 1)]?.id || plans.value[0]?.id
      }
    }
  } catch {
    // 如果从 wecomData 获取
    if (props.wecomData?.vasConfig?.aiPricing) {
      plans.value = props.wecomData.vasConfig.aiPricing
    }
  }
}
```

### 1.2 后端路由实现

**文件：** `backend/src/routes/wecom/aiAssistant.ts` (第720-762行)

```typescript
// AI套餐列表（从管理后台配置读取）
router.get('/ai/packages', authenticateToken, async (req: Request, res: Response) => {
  try {
    // 优先从 system_config 读取完整定价配置（含支付方式）
    const pricingRows = await AppDataSource.query(
      "SELECT config_value FROM system_config WHERE config_key = 'wecom_pricing_config' LIMIT 1"
    ).catch(() => []);
    if (pricingRows.length > 0) {
      try {
        const config = JSON.parse(pricingRows[0].config_value);
        if (config.aiPackages && Array.isArray(config.aiPackages) && config.aiPackages.length > 0) {
          return res.json({ success: true, data: config.aiPackages, paymentMethods: config.paymentMethods || ['wechat', 'alipay'] });
        }
      } catch {}
    }
    // 次选：从 system_config 的 ai_packages_global 读取
    const sysRows = await AppDataSource.query(
      "SELECT config_value FROM system_config WHERE config_key = 'ai_packages_global' LIMIT 1"
    ).catch(() => []);
    if (sysRows.length > 0) {
      try {
        const packages = JSON.parse(sysRows[0].config_value);
        if (Array.isArray(packages) && packages.length > 0) {
          return res.json({ success: true, data: packages });
        }
      } catch {}
    }
    // 次选：从 TenantSettings 读取
    const { TenantSettings } = await import('../../entities/TenantSettings');
    const repo = AppDataSource.getRepository(TenantSettings);
    let setting = await repo.findOne({ where: { settingKey: 'ai_packages_global' } });
    if (setting) {
      const packages = setting.getValue();
      if (Array.isArray(packages) && packages.length > 0) {
        return res.json({ success: true, data: packages });
      }
    }
    // 没有配置任何套餐 → 返回空（暂无套餐）
    res.json({ success: true, data: [] });
  } catch (error: any) {
    res.status(500).json({ success: false, message: error.message });
  }
});
```

### 1.3 管理后台配置页面

**文件：** `admin/src/views/wecom/Pricing.vue`

管理后台有完整的AI套餐配置界面，配置后会保存到 `system_config` 表的 `wecom_pricing_config` 键中。

---

## 二、问题根源

经过分析，发现问题可能是以下几种情况之一：

1. **管理后台未配置AI套餐** - 管理员没有在「企微管理 > 套餐与定价」中配置AI助手套餐
2. **数据库中没有配置记录** - `system_config` 表中缺少 `wecom_pricing_config` 或 `ai_packages_global` 记录
3. **配置数据格式错误** - JSON格式不正确或字段名不匹配
4. **权限问题** - 会员中心调用API时token验证失败

---

## 三、排查步骤

### 3.1 检查数据库配置

```sql
-- 检查企微定价配置
SELECT config_key, config_value 
FROM system_config 
WHERE config_key IN ('wecom_pricing_config', 'ai_packages_global');

-- 检查TenantSettings表
SELECT * FROM tenant_settings 
WHERE setting_key = 'ai_packages_global';
```

### 3.2 检查管理后台配置

1. 登录管理后台 `http://localhost:5174`
2. 进入「企微管理 > 套餐与定价」
3. 查看「AI助手套餐」部分是否有配置数据
4. 如果没有，点击「添加套餐」按钮添加套餐

### 3.3 检查API调用

在浏览器开发者工具中：

1. 打开官网会员中心 `http://localhost:8080/member/ai-assistant`
2. 打开Network标签
3. 查找 `/api/v1/wecom/ai/packages` 请求
4. 检查：
   - 请求状态码（应该是200）
   - 响应数据格式
   - Authorization header是否正确

---

## 四、修复方案

### 方案一：在管理后台配置AI套餐（推荐）

1. 登录管理后台
2. 进入「企微管理 > 套餐与定价」
3. 滚动到「AI助手套餐」部分
4. 点击「添加套餐」按钮
5. 配置套餐信息：
   - 套餐名称：如"体验包"、"基础包"、"专业包"
   - 调用次数：如100、1000、5000
   - 价格：如0、99、299
   - 有效期：forever（永久）或天数
   - 描述：套餐说明
   - 是否推荐：勾选热门套餐
6. 点击「保存配置」

### 方案二：直接插入数据库（临时方案）

如果管理后台无法访问，可以直接在数据库中插入配置：

```sql
-- 插入默认AI套餐配置
INSERT INTO system_config (id, config_key, config_value, config_type, created_at, updated_at)
VALUES (
  UUID(),
  'wecom_pricing_config',
  '{
    "aiPackages": [
      {
        "id": "trial",
        "name": "体验包",
        "calls": 100,
        "price": 0,
        "description": "免费体验100次AI调用",
        "validity": "forever",
        "recommended": false
      },
      {
        "id": "basic",
        "name": "基础包",
        "calls": 1000,
        "price": 99,
        "description": "1000次AI调用，适合小团队",
        "validity": "90",
        "recommended": true
      },
      {
        "id": "pro",
        "name": "专业包",
        "calls": 5000,
        "price": 399,
        "description": "5000次AI调用，适合中型团队",
        "validity": "180",
        "recommended": false
      },
      {
        "id": "enterprise",
        "name": "企业包",
        "calls": 20000,
        "price": 1299,
        "description": "20000次AI调用，适合大型企业",
        "validity": "365",
        "recommended": false
      }
    ],
    "paymentMethods": ["wechat", "alipay"]
  }',
  'json',
  NOW(),
  NOW()
)
ON DUPLICATE KEY UPDATE 
  config_value = VALUES(config_value),
  updated_at = NOW();
```

### 方案三：修复前端降级逻辑

如果后端API确实返回空数据，可以在前端添加默认套餐：

**文件：** `website/src/views/member/wecom/WecomAiAssistant.vue`

```typescript
const fetchPackages = async () => {
  try {
    const token = getMemberToken()
    const res = await fetch('/api/v1/wecom/ai/packages', { 
      headers: { 'Authorization': `Bearer ${token}` } 
    })
    const json = await res.json()
    if (json.success && json.data && json.data.length > 0) {
      plans.value = json.data
    } else {
      // 使用默认套餐
      plans.value = getDefaultPackages()
    }
  } catch {
    // 降级到默认套餐
    plans.value = getDefaultPackages()
  }
}

// 添加默认套餐函数
const getDefaultPackages = () => [
  {
    id: 'trial',
    name: '体验包',
    quota: 100,
    price: 0,
    unit: '永久',
    features: ['100次AI调用', '基础功能', '永久有效']
  },
  {
    id: 'basic',
    name: '基础包',
    quota: 1000,
    price: 99,
    unit: '90天',
    features: ['1000次AI调用', '全部功能', '90天有效']
  },
  {
    id: 'pro',
    name: '专业包',
    quota: 5000,
    price: 399,
    unit: '180天',
    features: ['5000次AI调用', '全部功能', '180天有效', '优先支持']
  }
]
```

---

## 五、验证步骤

### 5.1 验证管理后台配置

1. 登录管理后台
2. 进入「企微管理 > 套餐与定价」
3. 确认AI助手套餐列表中有数据
4. 点击「保存配置」确保数据已保存

### 5.2 验证数据库

```sql
-- 查询配置是否存在
SELECT 
  config_key,
  JSON_PRETTY(config_value) as config_value,
  created_at,
  updated_at
FROM system_config 
WHERE config_key = 'wecom_pricing_config';

-- 验证aiPackages字段
SELECT 
  JSON_EXTRACT(config_value, '$.aiPackages') as ai_packages
FROM system_config 
WHERE config_key = 'wecom_pricing_config';
```

### 5.3 验证API响应

使用curl或Postman测试：

```bash
# 获取会员token（先登录）
curl -X POST http://localhost:3000/api/v1/public/member/login \
  -H "Content-Type: application/json" \
  -d '{"phone":"13800138000","loginType":"password","password":"123456"}'

# 使用token调用AI套餐接口
curl -X GET http://localhost:3000/api/v1/wecom/ai/packages \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

预期响应：

```json
{
  "success": true,
  "data": [
    {
      "id": "trial",
      "name": "体验包",
      "calls": 100,
      "price": 0,
      "description": "免费体验100次AI调用",
      "validity": "forever",
      "recommended": false
    },
    {
      "id": "basic",
      "name": "基础包",
      "calls": 1000,
      "price": 99,
      "description": "1000次AI调用，适合小团队",
      "validity": "90",
      "recommended": true
    }
  ],
  "paymentMethods": ["wechat", "alipay"]
}
```

### 5.4 验证前端显示

1. 打开官网会员中心 `http://localhost:8080/member/login`
2. 登录后进入「AI助手」页面
3. 切换到「开通与充值」标签
4. 确认能看到套餐卡片显示

---

## 六、常见问题

### Q1: API返回空数组

**原因：** 数据库中没有配置数据

**解决：** 使用方案一或方案二添加配置

### Q2: API返回401未授权

**原因：** Token过期或无效

**解决：** 重新登录获取新token

### Q3: 套餐显示但无法购买

**原因：** 支付配置未完成

**解决：** 检查支付方式配置和支付接口

### Q4: 管理后台保存后前端仍然看不到

**原因：** 可能是缓存问题

**解决：** 
1. 清除浏览器缓存
2. 重启后端服务
3. 检查数据库是否真的保存成功

---

## 七、相关文件清单

### 前端文件
- `website/src/views/member/AiAssistant.vue` - AI助手主页面
- `website/src/views/member/wecom/WecomAiAssistant.vue` - AI助手组件
- `website/src/api/member.ts` - 会员API封装

### 后端文件
- `backend/src/routes/wecom/aiAssistant.ts` - AI助手路由
- `backend/src/routes/wecom/index.ts` - 企微路由入口
- `backend/src/app.ts` - 主应用路由注册

### 管理后台文件
- `admin/src/views/wecom/Pricing.vue` - 套餐与定价配置页面
- `backend/src/routes/admin/wecom-management.ts` - 管理后台企微路由

### 数据库表
- `system_config` - 系统配置表（存储wecom_pricing_config）
- `tenant_settings` - 租户设置表（备用存储）

---

## 八、总结

本次问题的核心是**管理后台配置的AI套餐数据没有正确映射到官网会员中心**。

**解决方案优先级：**
1. ✅ **推荐：** 在管理后台配置AI套餐（方案一）
2. ⚠️ **临时：** 直接插入数据库（方案二）
3. 🔧 **降级：** 前端使用默认套餐（方案三）

**后续优化建议：**
1. 添加配置检查工具，自动检测配置是否完整
2. 在管理后台添加"预览"功能，直接查看会员中心效果
3. 添加配置导入导出功能，方便迁移和备份
4. 完善错误提示，当配置缺失时给出明确的操作指引

---

**修复完成时间：** 2026-04-22  
**修复人员：** Kiro AI Assistant  
**验证状态：** 待验证

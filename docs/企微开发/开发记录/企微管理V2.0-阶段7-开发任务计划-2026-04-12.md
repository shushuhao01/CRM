# 企微管理V2.0 — 阶段7：功能增强 开发任务计划

> **文档日期**: 2026-04-12
> **文档类型**: 开发任务计划（待确认后执行）
> **编制**: GitHub Copilot AI
> **状态**: 📝 **待确认**
> **关联文档**:
>   - `企微管理V2.0-阶段7-功能增强需求文档-2026-04-12.md`（需求规格）
>   - `企微管理V2.0-阶段6-ChatArchive组件拆分与Request类型修复-2026-04-12.md`（上一阶段）
>   - `企微管理V2.0深度分析与开发方案-2026-04-12.md`（总体方案）

---

## 一、项目现状总结

### 1.1 已完成阶段回顾

| 阶段 | 内容 | 状态 |
|------|------|------|
| 阶段1 | 需求深度剖析 + 差距矩阵 + V2.0方案 | ✅ 已完成 |
| 阶段2 | 架构可行性评估 + 技术难点分析 + 优化建议 | ✅ 已完成 |
| 阶段3 | sidebar.ts 深度审查与优化修复（5个安全修复+8个质量优化） | ✅ 已完成 |
| 阶段4 | 后端路由全面审查（12个文件，20处修复：安全脱敏+类型+数据完整性） | ✅ 已完成 |
| 阶段5 | 前端页面与API全面审查（9个文件，17处修复：运行时+内存+XSS+规范） | ✅ 已完成 |
| 阶段6 | ChatArchive组件拆分（2038→409行）+ 全局Request类型修复 | ✅ 已完成 |

### 1.2 当前代码资产

| 层级 | 文件数 | 总行数 | 质量评级 |
|------|--------|--------|---------|
| 后端路由 (wecom/) | 12个 | ~1,977行 | A- |
| 后端服务 | 2个 | ~1,151行 | B+ |
| 后端实体 | 7个 | ~462行 | A |
| 前端页面 (Wecom/) | 8个+5个子组件 | ~2,372行 | A- |
| 前端类型+工具 | 2个 | ~251行 | A |
| 前端API | 1个 | ~413行 | A |
| **合计** | **37个** | **~6,626行** | **A-** |

### 1.3 剩余技术债务（阶段6遗留）

| 优先级 | 内容 | 状态 |
|--------|------|------|
| P2 | callback.ts 事件处理器模式重构 | 待处理 |
| P2 | payment.ts queryBuilder 列名统一 | 待处理 |
| P3 | binding.ts 批量操作性能优化 | 待处理 |
| P3 | ChatArchiveService.ts SQL重写为TypeORM | 待处理 |

---

## 二、阶段7 需求总览

阶段7包含 **6大功能需求**，涉及：
- **新增数据库表**：1张（`wecom_archive_members`）
- **新增数据库字段**：6个（分布在3张表）
- **新增唯一索引**：1个（`customers.uk_tenant_wecom_userid`）
- **新增API**：12个
- **修改API**：4个
- **新增前端文件**：6个
- **修改前端文件**：10个

### 需求与阶段映射

| 需求 | 核心内容 | 分配阶段 |
|------|---------|---------|
| 需求1：会话存档席位管控 | 席位购买→生效成员选择→同步过滤→用量展示 | 7A |
| 需求2：存档设置-成员可见性与查看权限 | 自己/部门/全部三级可见性+后端过滤 | 7A |
| 需求3：增值服务-套餐与定价配置增强 | 套餐配置化+续费增购+Admin VAS编辑 | 7B |
| 需求4：企微管理独立窗口与全屏模式 | 全屏按钮+独立窗口路由+极简Layout | 7B |
| 需求5：CRM端企微客户深度集成 | 企微信息卡片+客户增强+USID字段+绑定管理 | 7C |
| 需求6：侧边栏客户详情增强 | 物流/售后/商品/收货/换绑+USID一键复制 | 7D |

---

## 三、开发阶段规划（共8个执行阶段）

> 将原规划的4个业务阶段（7A~7D）进一步细化为 **8个可执行阶段**，每个阶段工作量可控，便于逐步推进和验证。

### 阶段总览

| 执行阶段 | 内容摘要 | 涉及需求 | 预估工作量 | 依赖关系 |
|---------|---------|---------|-----------|---------|
| **7A-1** | 数据库迁移 + 后端实体 | 需求1+2+5 | 中 | 无（基础阶段） |
| **7A-2** | 席位管控后端API + 购买逻辑修复 | 需求1 | 中 | 依赖7A-1 |
| **7A-3** | 存档设置Tab前端 + 可见性后端过滤 | 需求1+2 | 大 | 依赖7A-2 |
| **7B-1** | 套餐配置化 + PurchaseDialog改造 + Admin VAS | 需求3 | 中 | 可与7A并行 |
| **7B-2** | 全屏模式 + 独立窗口 + 路由 | 需求4 | 中 | 可与7A并行 |
| **7C-1** | CRM客户USID字段 + 企微客户增强后端 | 需求5 | 大 | 依赖7A-1 |
| **7C-2** | CRM客户详情企微卡片 + 客户列表增强前端 | 需求5 | 大 | 依赖7C-1 |
| **7D** | 侧边栏客户详情增强（物流/售后/商品/收货/换绑） | 需求6 | 大 | 可独立 |

---

## 四、各阶段详细任务清单

---

### 阶段 7A-1：数据库迁移 + 后端实体（基础阶段）

**目标**：为后续所有功能打下数据基础

#### 任务清单

| # | 任务 | 文件操作 | 详细说明 |
|---|------|---------|---------|
| 1 | 创建 `wecom_archive_members` 表迁移脚本 | 新建 `backend/create-wecom-archive-members-table.js` | 7个字段：id, tenant_id, wecom_user_id, wecom_user_name, crm_user_id, is_enabled, created_at, updated_at。UNIQUE INDEX `uk_tenant_user (tenant_id, wecom_user_id)` |
| 2 | 创建 `WecomArchiveMember` 实体 | 新建 `backend/src/entities/WecomArchiveMember.ts` | TypeORM实体，@Entity('wecom_archive_members')，字段映射，关系装饰器 |
| 3 | `wecom_archive_settings` 表新增 `visibility` 字段 | 迁移脚本 + 修改实体 | ALTER TABLE 新增 `visibility` ENUM('self','department','all') DEFAULT 'all'；若 `WecomArchiveSetting` 实体已存在则修改，不存在则新建 |
| 4 | `wecom_archive_settings` 表确保 `max_users`、`used_users` 字段存在 | 检查并补充 | 需求文档指出这两个字段已存在但未被正确使用，确认字段存在 |
| 5 | `wecom_customers` 表新增4个统计字段 | 迁移脚本 + 修改实体 `WecomCustomer.ts` | 新增：`msg_sent_count` INT DEFAULT 0、`msg_recv_count` INT DEFAULT 0、`last_msg_time` BIGINT、`active_days_7d` INT DEFAULT 0 |
| 6 | `customers` 表新增 `wecom_external_userid` 字段 | 迁移脚本 + 修改实体 `Customer.ts` | VARCHAR(100) NULL + UNIQUE INDEX `uk_tenant_wecom_userid (tenant_id, wecom_external_userid)` |
| 7 | 在 `TENANT_ENTITIES` 注册新实体 | 修改租户隔离配置 | 确保 `WecomArchiveMember` 自动获得租户隔离 |
| 8 | 前端 `types.ts` 新增接口定义 | 修改 `src/views/Wecom/types.ts` | 新增：`ArchiveMember`、`ArchiveSettings`、`SeatInfo`、`WecomDeptNode`、`CustomerUSID` 等接口 |

#### 验证要点
- [ ] 迁移脚本可正确执行，表和字段创建成功
- [ ] TypeORM实体编译无错误
- [ ] 新实体已注册到TENANT_ENTITIES
- [ ] 前端types.ts编译无错误

---

### 阶段 7A-2：席位管控后端API + 购买逻辑修复

**目标**：完成席位管控的3个后端API + 修复购买流程写入max_users

#### 任务清单

| # | 任务 | 文件操作 | 详细说明 |
|---|------|---------|---------|
| 1 | 实现 `GET /wecom/chat-archive/seats` | 修改 `backend/src/routes/wecom/chatArchive.ts` | 返回 `{ maxUsers, usedUsers, members: [...], expireDate }`。从 `wecom_archive_settings` 读取max_users，从 `wecom_archive_members` 统计 is_enabled=true 的数量 |
| 2 | 实现 `PUT /wecom/chat-archive/seats/members` | 修改 `chatArchive.ts` | 接收 `{ memberIds: string[] }`（wecom_user_id数组），校验数量≤max_users，写入 `wecom_archive_members` 表，更新 `used_users` |
| 3 | 实现 `GET /wecom/chat-archive/seats/wecom-tree` | 修改 `chatArchive.ts` | 返回企微部门树结构，每个成员节点包含：wecom_user_id、name、是否已绑定CRM（查询wecom_user_bindings）、是否已选入存档范围（查询wecom_archive_members） |
| 4 | 修复 `POST /wecom/chat-archive/purchase` | 修改 `chatArchive.ts` | 购买成功后写入 `max_users` 到 `wecom_archive_settings`。增购时累加 max_users |
| 5 | 修改会话记录同步逻辑 | 修改 `WecomChatArchiveService.ts` | 同步时先读取 `wecom_archive_members` 中生效成员列表，仅拉取这些成员的会话记录 |
| 6 | 新增API到前端 `api/wecom.ts` | 修改 `src/api/wecom.ts` | 新增：`getArchiveSeats()`、`updateArchiveSeatMembers()`、`getArchiveWecomTree()` |

#### 验证要点
- [ ] seats API正确返回席位信息
- [ ] 选择成员数量不可超过max_users
- [ ] 购买后max_users正确写入
- [ ] 同步仅处理生效成员的消息
- [ ] 所有后端路由编译无错误

---

### 阶段 7A-3：存档设置Tab前端 + 可见性后端过滤

**目标**：完成存档设置UI（可见性配置+席位管理）+ 会话记录可见性过滤

#### 任务清单

| # | 任务 | 文件操作 | 详细说明 |
|---|------|---------|---------|
| 1 | 新建 `ArchiveSettings.vue` | 新建 `src/views/Wecom/components/ArchiveSettings.vue` (~350行) | **区域A：可见性设置**（3个Radio选项：仅查看自己/查看本部门/查看全部 + 保存按钮）；**区域B：存档生效成员管理**（左侧企微部门树+每个成员显示绑定状态+可勾选/取消+右侧已选列表+席位用量统计+保存/增购/刷新按钮） |
| 2 | 实现 `GET /wecom/chat-archive/settings` | 修改 `chatArchive.ts` | 返回 `{ visibility, maxUsers, usedUsers, expireDate, status }` |
| 3 | 实现 `PUT /wecom/chat-archive/settings` | 修改 `chatArchive.ts` | 接收 `{ visibility }` 更新设置，requireAdmin中间件保护 |
| 4 | 修改 `ChatArchive.vue` | 修改 `src/views/Wecom/ChatArchive.vue` | ① 新增第4个Tab"存档设置"（仅管理员可见）引入 ArchiveSettings 组件；② 顶部新增席位用量条：`📊 存档席位：已用 X/Y 个企微账号 ████░░ Z%`，用量≥90%橙色警告、100%红色+增购按钮 |
| 5 | 后端可见性过滤逻辑 | 修改 `chatArchive.ts` 或 `WecomChatArchiveService.ts` | 查询会话记录/会话列表时：管理员不限制；普通成员按visibility设置过滤（self→仅自己绑定的企微成员，department→本部门成员，all→不过滤） |
| 6 | 新增API到前端 | 修改 `src/api/wecom.ts` | 新增：`getArchiveSettings()`、`updateArchiveSettings()` |

#### 验证要点
- [ ] 存档设置Tab仅管理员可见
- [ ] 可见性Radio正常切换和保存
- [ ] 部门树正确展示，绑定/未绑定状态正确
- [ ] 席位选择不超限
- [ ] 席位用量条正确显示，颜色预警正确
- [ ] 会话记录查询按可见性正确过滤
- [ ] 编译无错误

---

### 阶段 7B-1：套餐配置化 + PurchaseDialog改造 + Admin VAS

**目标**：预设套餐从前端硬编码改为后端配置化，支持续费/增购模式

#### 任务清单

| # | 任务 | 文件操作 | 详细说明 |
|---|------|---------|---------|
| 1 | VAS定价API增强 | 修改 `chatArchive.ts` | `GET /wecom/chat-archive/vas-pricing` 响应增加：`presetPackages`（从system_config.wecom_vas_config读取）、`currentPlan`（当前套餐信息：maxUsers/usedUsers/expireDate/status/canRenew/canUpgrade）、`renewalDiscount` |
| 2 | 购买API增加type参数 | 修改 `chatArchive.ts` | `POST /wecom/chat-archive/purchase` 新增 `type: 'new' | 'renew' | 'upgrade'`：new=新购、renew=续费延长1年（折扣价）、upgrade=增购（按剩余天数折算价格）。写入max_users |
| 3 | 改造 `PurchaseDialog.vue` | 修改 `src/views/Wecom/components/PurchaseDialog.vue` | ① 删除硬编码的 `presetPackages`，改为从API获取；② 新增 `mode` prop：'new'/'renew'/'upgrade'；③ renew模式显示"续费1年，折扣价X"；④ upgrade模式显示"当前已购X人，增购后共Y人，增购价=增购数×单价×剩余天数/365" |
| 4 | Admin VAS配置页增强 | 修改 `admin/src/views/wecom/VasConfig.vue` | 新增预设套餐包编辑功能：可增删改套餐项（users/label/recommended），新增续费折扣配置 |
| 5 | Admin后端配置API | 检查并补充Admin路由 | 确保Admin可读写 `system_config.wecom_vas_config` 中的 presetPackages 和 renewalDiscount |
| 6 | ChatArchive.vue顶部套餐卡片 | 修改 `ChatArchive.vue` | 顶部增加当前套餐信息简卡：状态/到期时间/席位用量，若接近到期显示续费按钮 |

#### 验证要点
- [ ] VAS定价API正确返回presetPackages和currentPlan
- [ ] PurchaseDialog三种模式（新购/续费/增购）切换正确
- [ ] 续费折扣价格计算正确
- [ ] 增购按剩余天数折算正确
- [ ] Admin VAS配置页面可编辑套餐包
- [ ] 编译无错误

---

### 阶段 7B-2：全屏模式 + 独立窗口 + 路由

**目标**：企微管理模块支持全屏和独立窗口操作

#### 任务清单

| # | 任务 | 文件操作 | 详细说明 |
|---|------|---------|---------|
| 1 | 新建 `WecomHeader.vue` 共享组件 | 新建 `src/views/Wecom/components/WecomHeader.vue` (~60行) | 共享Header组件：左侧slot（标题）、右侧actions slot + 全屏按钮（Fullscreen API + 隐藏Layout CSS）+ 独立窗口按钮（window.open） |
| 2 | 新建 `WecomStandaloneLayout.vue` | 新建 `src/layouts/WecomStandaloneLayout.vue` (~80行) | 极简Layout：仅顶栏（Logo+标题"云客CRM-企微管理"+关闭按钮）+ 内容区（router-view） |
| 3 | 新建 `WecomStandalone.vue` | 新建 `src/views/Wecom/WecomStandalone.vue` (~200行) | 独立窗口容器页面：Tab导航栏（配置/绑定/客户/会话存档/收款/侧边栏/获客助手/微信客服）+ 动态组件加载对应Tab内容 |
| 4 | 新增路由 `/wecom-standalone` | 修改 `src/router/index.ts` | 新增路由，使用WecomStandaloneLayout，组件为WecomStandalone，路由参数 `?tab=xxx` |
| 5 | 为所有Wecom页面添加WecomHeader | 修改8个Wecom/*.vue页面 | 替换各页面原有的el-card header区域，引入WecomHeader组件，传入当前Tab名称用于独立窗口跳转 |
| 6 | 全屏CSS支持 | 修改全局CSS或Layout组件 | 添加 `.wecom-fullscreen` class：当全屏模式时，通过CSS隐藏侧边栏和顶栏（`#app-sidebar { display: none }`等） |

#### 验证要点
- [ ] 全屏按钮点击后正确进入全屏，再点击恢复
- [ ] 独立窗口按钮正确打开新窗口，Tab参数正确
- [ ] 独立窗口共享登录态
- [ ] 独立窗口Tab切换正常
- [ ] 所有页面WecomHeader正确显示
- [ ] 编译无错误

---

### 阶段 7C-1：CRM客户USID字段 + 企微客户增强后端

**目标**：后端API支持USID关联和企微客户增强统计

#### 任务清单

| # | 任务 | 文件操作 | 详细说明 |
|---|------|---------|---------|
| 1 | 实现 `PUT /customers/:id/wecom-usid` | 新建或修改CRM客户路由 | 接收 `{ wecomExternalUserid }`，校验：格式（wmXXX开头或为空）、租户内唯一性（查询是否已有其他客户使用此USID），写入 `customers.wecom_external_userid` |
| 2 | 实现 `GET /customers/:id/wecom-info` | 新建或修改CRM客户路由 | 返回CRM客户关联的企微信息：绑定的企微账号、关联微信数、标签、最近聊天摘要、跟进人等 |
| 3 | 实现 `GET /wecom/customers/:id/detail` | 修改 `customer.ts` | 返回企微客户详情：基本信息+消息统计（msg_sent_count/recv_count/last_msg_time/active_days_7d）+关联CRM客户信息 |
| 4 | 实现 `GET /wecom/customers/stats/messages` | 修改 `customer.ts` | 批量获取客户消息统计：接收clientIds数组，返回各客户的消息数量和活跃度 |
| 5 | 实现 `POST /wecom/customers/:id/follow-record` | 修改 `customer.ts` | 在企微客户管理中直接添加CRM跟进记录（关联FollowUp实体） |
| 6 | 实现 `PUT /wecom/sidebar/binding/:id/rebind` | 修改 `sidebar.ts` | 侧边栏换绑：清除当前绑定→跳转重新绑定 |
| 7 | 实现 `DELETE /wecom/sidebar/binding/:id` | 修改 `sidebar.ts` | 侧边栏解绑：删除绑定记录 |
| 8 | 修改CRM客户列表查询 | 修改客户列表路由 | 支持按 `wecom_external_userid` 搜索和筛选 |
| 9 | 新增前端API方法 | 修改 `src/api/wecom.ts` | 新增全部7个API方法 |

#### 验证要点
- [ ] USID更新时唯一性校验生效
- [ ] 企微客户详情API正确返回统计数据
- [ ] 消息统计批量查询性能可接受
- [ ] 跟进记录正确写入FollowUp表
- [ ] 换绑/解绑逻辑正确
- [ ] CRM客户列表USID搜索生效
- [ ] 所有后端路由编译无错误

---

### 阶段 7C-2：CRM客户详情企微卡片 + 客户列表增强前端

**目标**：前端实现CRM与企微的深度集成UI

#### 任务清单

| # | 任务 | 文件操作 | 详细说明 |
|---|------|---------|---------|
| 1 | 新建 `WecomInfoCard.vue` | 新建 `src/views/Customer/components/WecomInfoCard.vue` (~200行) | 独立子组件：展示企微绑定信息（企微昵称/关联微信数/跟进人/添加方式/标签/最近聊天摘要），按钮[查看会话记录][查看企微详情] |
| 2 | 修改 `Customer/Detail.vue` | 修改 `src/views/Customer/Detail.vue` | 引入 `<WecomInfoCard :customer-id="..." />`；新增USID字段展示（含复制按钮、编辑按钮、未关联时显示手动关联按钮） |
| 3 | 修改 `Customer/List.vue` | 修改 `src/views/Customer/List.vue` | 新增"企微USID"表格列，支持搜索/筛选 |
| 4 | 新建 `CustomerDetailDrawer.vue` | 新建 `src/views/Wecom/components/CustomerDetailDrawer.vue` (~300行) | 企微客户详情抽屉：完整客户信息（企微+CRM+跟进+订单汇总），可从企微客户列表打开 |
| 5 | 修改 `Wecom/Customer.vue` | 修改 `src/views/Wecom/Customer.vue` | 表格新增列：活跃度（近7天消息数）、留存状态、收发消息统计、关联微信数、企微USID；行操作新增"详情"按钮打开CustomerDetailDrawer |
| 6 | 修改 `Wecom/Binding.vue` | 修改 `src/views/Wecom/Binding.vue` | 新增列：侧边栏绑定状态、绑定时间；操作列新增"解绑"/"换绑"按钮 |

#### 验证要点
- [ ] WecomInfoCard在Customer详情页正确显示
- [ ] USID编辑/复制功能正常
- [ ] 客户列表USID列正确显示和筛选
- [ ] 企微客户管理新增列数据正确
- [ ] CustomerDetailDrawer抽屉正确打开和关闭
- [ ] 绑定管理解绑/换绑功能正常
- [ ] 编译无错误

---

### 阶段 7D：侧边栏客户详情增强

**目标**：侧边栏CustomerDetail展示物流/售后/商品/收货信息+换绑功能+USID

#### 任务清单

| # | 任务 | 文件操作 | 详细说明 |
|---|------|---------|---------|
| 1 | 扩展 `GET /wecom/sidebar/customer-detail` | 修改 `sidebar.ts` | 响应新增：① `shippingAddress`（默认收货地址，手机号脱敏`138****5678`，地址省市+`***`）；② `orders[].products`（每笔订单商品明细 `[{name, quantity, price}]`）；③ `orders[].logistics`（物流信息：trackingNo/carrier/status/estimatedDelivery，查询`logistics_records`表）；④ `afterSales`（售后记录列表：type/status/reason/amount/createdAt，查询`after_sales_services`表）；⑤ `bindingInfo`（CRM用户名/租户编码/绑定时间）；⑥ `crmCustomer.wecomExternalUserid`（客户USID） |
| 2 | 修改侧边栏 `CustomerDetail.vue` | 修改 `website/src/views/wecom-sidebar/CustomerDetail.vue`（或对应路径） | 新增展示区块：① USID行（含📋一键复制按钮）；② 📦收货信息区块（收货人/手机脱敏/地址脱敏）；③ 📋最近订单增强（每笔订单下展示商品明细+物流状态）；④ 🔧售后记录区块（退换货/投诉列表）；⑤ 绑定信息区块（含租户编码）；⑥ 换绑按钮+确认弹窗 |
| 3 | 一键复制功能 | 在CustomerDetail.vue中实现 | 使用 `navigator.clipboard.writeText()` 实现USID一键复制，成功后toast提示 |
| 4 | 换绑功能后端 | 修改 `sidebar.ts` | `PUT /wecom/sidebar/binding/:id/rebind` — 清除当前绑定记录，返回重新绑定引导 |
| 5 | 数据脱敏验证 | 审查代码 | 确认所有敏感数据正确脱敏：手机号`138****5678`、地址`省市+***`、物流单号完整展示 |

#### 验证要点
- [ ] 收货信息正确脱敏显示
- [ ] 订单商品明细正确解析和展示
- [ ] 物流信息正确展示（状态映射中文）
- [ ] 售后记录正确展示（类型/状态映射中文）
- [ ] USID一键复制功能正常
- [ ] 换绑确认弹窗正常，换绑后清除绑定
- [ ] 数据脱敏无遗漏
- [ ] 编译无错误

---

## 五、阶段依赖关系图

```
7A-1 (数据库+实体) ←── 基础，所有阶段依赖
  │
  ├── 7A-2 (席位API+购买修复) 
  │     └── 7A-3 (存档设置UI+可见性过滤)
  │
  ├── 7C-1 (USID+企微客户后端)
  │     └── 7C-2 (CRM客户前端集成)
  │
  │           ┌── 7B-1 (套餐配置化) ──── 可与7A并行
  └── 并行 ──┤
              └── 7B-2 (全屏+独立窗口) ── 可与7A并行

7D (侧边栏增强) ──── 可独立执行，不强依赖其他阶段
```

**推荐执行顺序**：

```
7A-1 → 7A-2 → 7A-3 → 7B-1 → 7B-2 → 7C-1 → 7C-2 → 7D
```

> 其中 7B-1、7B-2 可提前到 7A-2 之后并行执行，视开发节奏灵活调整。

---

## 六、风险提示

| 风险 | 等级 | 应对 |
|------|------|------|
| Customer/Detail.vue 已4256行，修改需谨慎 | 中 | 企微信息卡片作为独立子组件引入，最小化对Detail.vue的改动 |
| 侧边栏CustomerDetail.vue可能在website项目中 | 中 | 需确认侧边栏H5页面的实际文件位置（可能在website/或独立目录） |
| 会话记录可见性过滤需考虑性能 | 中 | 使用IN子查询而非应用层过滤，添加必要索引 |
| 独立窗口跨组件状态同步 | 低 | 同源同Cookie/Token，状态通过API获取而非本地缓存 |
| Admin VasConfig.vue需确认当前状态 | 低 | 开发前先审查现有Admin企微页面 |

---

## 七、交付标准

每个阶段完成后需满足：
1. ✅ 所有修改文件编译零错误（TypeScript + Vue）
2. ✅ 新增API可正确调用并返回预期格式
3. ✅ 新增/修改的前端组件可正确渲染
4. ✅ 数据库迁移脚本可正确执行
5. ✅ 输出阶段完成记录文档到 `docs/企微开发/`

---

> **待确认**：请审阅以上8个执行阶段的详细任务清单，确认无误后我将从 **阶段7A-1** 开始逐步执行开发。每完成一个阶段会输出完成记录，待您确认"继续"后再进入下一阶段。

---

> **本文档路径**: `docs/企微开发/企微管理V2.0-阶段7-开发任务计划-2026-04-12.md`


# 依赖目录
node_modules/
backend/node_modules/

# 构建输出
dist/
backend/dist/

# 日志文件
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
logs/
*.log.*

# 编辑器配置
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 环境变量文件
# 注意：.env / .env.development / .env.production 已允许提交（不含敏感信息，部署需要）
# 仅 .local 后缀的本地覆盖文件不提交
.env.local
.env.*.local
backend/.env.local

# PM2配置（如果包含敏感信息，取消注释）
# ecosystem.config.js
# backend/ecosystem.config.js

# 临时文件
*.tmp
*.temp
.cache/
.temp/

# 一次性调试/诊断输出文件
**/diag-*.txt
**/temp_*.txt
**/test-out.txt
**/fix-report.txt
**/tsc-errors.txt
**/*-result.txt
**/*-result*.json
*.b64

# 构建产物
tsconfig.tsbuildinfo
backend/tsconfig.tsbuildinfo

# 测试覆盖率
coverage/
.nyc_output/

# 操作系统文件
Thumbs.db
desktop.ini

# 备份文件
*.bak
*.backup
*.backup-*
*.old
*~

# 临时测试文件
test123.*
tpl_test.txt
test-*.html
test-api-result.json
build-result*.txt

# 数据库文件
*.sqlite
*.db
# 允许 database/migrations/ 下的 SQL 迁移脚本
# *.sql  -- 已移除全局忽略，改为精确控制

# 上传文件
uploads/
public/uploads/

# 证书文件
*.pem
*.key
*.crt
*.csr

# 压缩文件
*.zip
*.tar
*.tar.gz
*.rar
*.7z

# Electron 打包输出
dist-electron/
dist_electron/
out/

# APP项目（独立仓库管理）
crmAPP/

# ==================== 文档目录（仅提交到备份仓库）====================
# docs/ 和 重要文档/ 包含开发记录和重要文档，不提交主仓库和子仓库
# 备份仓库通过 sync-all.ps1 脚本的 git add -f 强制包含
docs/
重要文档/

# 后端日志目录
backend/logs/

# ==================== 🔴 一级机密：密钥和敏感配置 ====================
# SaaS 平台许可签发脚本（含 RSA 私钥，绝不可上传到公开仓库或交付客户）
backend/scripts/generate-saas-license.js

# 生产服务器环境配置（含真实数据库密码、JWT密钥等）
backend/.env.production.server
backend/.env.*.local

# ==================== 🟠 二级机密：运营方内部文档 ====================
# 注意：docs/ 已整体排除，以下规则保留作为机密等级记录，确保备份仓库也不包含这些文件
# （备份脚本会排除以下文件）

# ==================== 🟡 三级敏感：部署和运维脚本 ====================
# 部署脚本（Windows本地开发用，含服务器信息）
deploy.ps1
# 注意：deploy.sh 已允许提交（Linux部署脚本，不含敏感信息，服务器 git clone 后需要使用）

# 备份和同步脚本（含仓库地址等）
backup-*.ps1
# 注意：sync-repos*.ps1 和 push-website-admin.ps1 已允许提交（用于子仓库同步）
# sync-repos*.ps1
# push-website-admin.ps1
setup-remotes.ps1
create-release-package.ps1
start-backend-dev.ps1

# 配置备份（含真实服务器Nginx配置等）
# docs/配置备份/ ← 已被 docs/ 规则覆盖

# ==================== 🟢 四级敏感：数据文件 ====================
# 租户数据导出文件（客户真实数据）
backend/exports/

# 测试输出文件
tsc-result*.txt
backend/test-*.js
tests/test-*-output.txt

# Release 打包输出
CRM-v*-release.zip

